# Core API Requests Layer
requests>=2.31.0

# Natural Language Processing & Semantics
spacy>=3.7.0

# Environment and Secret Management
python-dotenv>=1.0.0

# After installing dependencies, run the following command in your terminal to download the lightweight English semantic processing model used by the tool:

python3 -m spacy download en_core_web_sm